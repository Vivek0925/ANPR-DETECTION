This is the Next.js frontend for the ANPR MVP.

See the top-level `README.md` (one directory up) for full setup,
architecture, and usage instructions.

Quick start:
```bash
npm install
cp .env.example .env.local
npm run dev
```
